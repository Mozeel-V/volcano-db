#include "planner/planner.h"

// Physical plan is the same as logical plan with algorithm annotations.
// The optimizer is responsible for choosing algorithms.
// This file is a placeholder for any future physical-plan-specific logic.

namespace planner {
// Currently PhysicalNodePtr = LogicalNodePtr, so no extra code needed.
}
